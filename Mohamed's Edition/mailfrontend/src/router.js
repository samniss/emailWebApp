/*import Vue from "vue";
import MailBox from "./components/MailBox";

Vue.use(Router);

const routes = [
    {
        path: "/",
        name: "mailbox",
        component: MailBox
    }
]
export default new Router({
    mode: "history",
    base: process.env.BASE_URL,
    routes
});
*/